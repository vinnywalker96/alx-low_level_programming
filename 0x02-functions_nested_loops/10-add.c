#include "main.h"

/**
 * add - adds 2 integers
 * @a: the first argument of the function
 * @b: the second argument of the function
 * Return: the result
 */

int add(int a, int b)
{
	return (a + b);
}
